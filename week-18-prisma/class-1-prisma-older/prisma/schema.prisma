generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL") 
  // ignore error, working fine! // Thanks to Cline!
}

model User {
  id       Int     @id @default(autoincrement())
  username String  @unique
  password String
  city     String?
}
